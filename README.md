# SKALA-FRONT

본 저장소는 **SKALA 과정**의 HTML, CSS, JavaScript 전 과정을 통합 수행한 프론트엔드 포털 프로젝트입니다. 
웹 표준 시맨틱 구조 설계부터 성능 최적화 레이아웃, 모바일 반응형 대응, 그리고 비동기 모듈러 아키텍처까지 프론트엔드 실무 역량 강화를 목표로 작성되었습니다.

---

## 🛠️ 개발 환경 및 실행 방법
본 프로젝트는 **ES6 JavaScript Module (`type="module"`)**을 사용하므로 CORS 정책 준수를 위해 로컬 개발 서버가 필요합니다.

*   **VS Code 사용 시**: `Live Server` 또는 `Five Server` 확장을 설치하여 `html/index.html`을 오픈합니다.
*   **CLI 환경 사용 시**:
    ```bash
    npx live-server html/index.html
    ```

---

## 📂 과제 및 미션 수행 리스트 (총 19개 과제)

### 1부. HTML 파트 (과제 1 ~ 과제 7)

#### HTML 과제 1: Project 구성과 index.html 생성
*   **기본 요구사항**
    *   [x] `html/index.html` 초기 파일 생성
    *   [x] 브라우저 타이틀(`<title>`)을 `"Welcome SKALA"`로 설정
    *   [x] 본문 타이틀(`<h1>`)에 `"환영 인사"` 작성 및 본문(`<p>`)에 `"스칼라에 오신 것을 환영합니다."` 작성
*   **추가 실습사항**
    *   [x] 메인 레이아웃 적용을 위한 글로벌 초기화 마크업 사전 구성

---

#### HTML 과제 2: 나의 휴일 일과
*   **기본 요구사항**
    *   [x] `html/holiday.html` 파일 생성
    *   [x] 필수 태그(`<h1>`, `<h2>`, `<br>`, `<p>`, `<mark>`)를 사용하여 휴일 일과 기록
*   **추가 실습사항**
    *   [x] 가독성 향상을 위해 일과 시간대별로 정보를 그룹화하고 하이라이트 요소 정의

---

#### HTML 과제 3: 나의 소개
*   **기본 요구사항**
    *   [x] `html/myProfile.html` 파일 생성
    *   [x] 필수 태그(`<ul>`로 좋아하는 음식 리스트, `<ol>`로 올해 할 일 리스트, `<dl>`로 본인 설명 정의) 활용
    *   [x] 스타일링(CSS)은 배제하고 순수 구조 마크업으로 작성
*   **추가 실습사항**
    *   [x] 시맨틱 구조 준수를 위해 설명 목록(`<dl>`) 태그의 `<dt>`, `<dd>` 관계 정교화

---

#### HTML 과제 4: 나의 강의 일정
*   **기본 요구사항**
    *   [x] `html/myClass.html` 파일 생성
    *   [x] 필수 태그(`<table>`, `<thead>` 시간/요일 지정, `<tbody>`, `<td>` 병합) 활용
    *   [x] 2시간 이상의 연속 강의 및 점심시간에 대해 `colspan`/`rowspan` 셀 병합 적용
    *   [x] CSS 스타일 적용 배제
*   **추가 실습사항**
    *   [x] 스크린 리더 지원을 위해 `<caption>` 태그 및 `scope` 속성을 테이블 헤더에 적용하여 접근성 확보

---

#### HTML 과제 5: 바로가기
*   **기본 요구사항**
    *   [x] `html/index.html` 파일에 나의 수업, 휴일, 프로필 바로가기 메뉴 추가
    *   [x] 필수 태그(`<a>` 하이퍼링크) 활용
    *   [x] CSS 스타일 적용 배제
*   **추가 실습사항**
    *   [x] 상대 경로(`/html/...`) 참조 관계의 정합성을 검증하여 데드링크(Broken Link) 방지

---

#### HTML 과제 6: 나의 여행지
*   **기본 요구사항**
    *   [x] `html/myTrip.html` 파일 생성 및 `media/` 폴더 구성
    *   [x] 필수 태그(`<audio>`와 `<source>`, `<img>`, `<video>`와 `<source>`)를 사용한 멀티미디어 여행기 작성
*   **추가 실습사항**
    *   [x] 미디어 로딩 성능 향상을 위해 대체 이미지 텍스트(`alt`) 제공 및 비디오 코덱(MP4) 호환성 확보

---

#### HTML 과제 7: 포털 사이트형 메인 Hub 만들기
*   **기본 요구사항**
    *   [x] `html/index.html` 파일 개편하여 기존 작업물(강의 일정, 휴일 일과, 프로필, 여행지, 회원가입)을 집약한 허브 구축
    *   [x] 필수 시맨틱 태그(`<nav>` 메뉴 네비게이션, `<main>` 주요 콘텐츠 영역, `<aside>` 사이드바 부가 정보) 적용
*   **추가 실습사항**
    *   [x] 웹 브라우저의 파싱 최적화를 위해 시맨틱 태그 간 중첩 관계를 정리하고 페이지 정보 구조 고도화

---

### 2부. CSS 파트 (과제 8 ~ 과제 13)

#### CSS 과제 8: 미션1 - 전체 테마 및 텍스트 Styling
*   **기본 요구사항**
    *   [x] 단일 외부 스타일시트 `/css/style.css` 생성 및 모든 HTML 파일에 `<link>` 태그 연결
    *   [x] 구글 웹 폰트(Google Fonts) 연동 및 `body` 선택자를 통한 글로벌 서체 적용
    *   [x] 글로벌 줄간격(`line-height`), 텍스트 색상(`color`), 배경색(`background-color`) 정의
    *   [x] `h1`, `h2` 선택자 스타일링(크기, 여백, 테두리) 및 하이퍼링크(`a`) 컬러/밑줄 스타일 제어
*   **추가 실습사항**
    *   [x] 브라우저 간 일관된 렌더링을 위해 기본적인 마진/패딩 여백 초기화(CSS Reset) 규칙 사전 적용

---

#### CSS 과제 9: 미션2 - 박스 모델의 이해
*   **기본 요구사항**
    *   [x] 전체 콘텐츠를 중앙 정렬하기 위해 `body` 직하단에 `<div class="container">` 레이아웃 래퍼(Wrapper) 추가 및 스타일링
    *   [x] `myTrip.html`: 여행지 단락에 `.trip-card` 클래스를 부여하고 박스 모델(배경색, 테두리, 패딩, 마진, 그림자)을 조정해 카드 뷰 스타일 디자인
    *   [x] `myClass.html`: `table`, `th`, `td` 스타일을 다듬어 경계선 및 가독성이 확보된 표 디자인 구축
*   **추가 실습사항**
    *   [x] 테이블 셀 병합 시 테두리가 겹치지 않도록 `border-collapse: collapse` 지정 및 카드 간 균등 배치 마진 마이크로 튜닝

---

#### CSS 과제 10: 미션3 - 가독성 높은 회원가입 폼
*   **기본 요구사항**
    *   [x] `html/signUp.html` 및 `html/signUpResult.html` 생성
    *   [x] 입력 창(`input`, `select`, `textarea`) 크기 확장 및 포커스 상태 스타일링
    *   [x] `fieldset` 그룹 테두리 스타일 커스터마이징 및 범례(`legend`) 강조
    *   [x] 회원가입 제출 및 초기화 버튼 커스텀 스타일링
*   **추가 실습사항**
    *   [x] SK AX 브랜드 정체성을 투영한 그라데이션 제출 버튼 구현 및 입력 오류 시 포커스 링 애니메이션 추가

---

#### CSS 과제 11: 미션4 - Flex와 Grid로 레이아웃 잡기
*   **기본 요구사항**
    *   [x] `index.html`: 상단 바로가기 메뉴 리스트에 Flexbox를 적용하여 균등 배치 및 `main`과 `aside` 가로 2단 배치 적용
    *   [x] `myTrip.html`: 여행지 카드 목록을 CSS Grid를 적용하여 3열 바둑판 배열(`grid-template-columns: repeat(3, 1fr)`)로 구현
*   **추가 실습사항**
    *   [x] Grid 아이템 간 간격 조정을 위한 `gap: 24px` 설계 및 카드 내용 높이 불균형 시 자동 정렬(`align-items: stretch`) 처리

---

#### CSS 과제 12: 미션5 - 스마트폰에서 보기 (반응형 웹 디자인)
*   **기본 요구사항**
    *   [x] 미디어 쿼리(`@media (max-width: 786px)`) 기준점 설계
    *   [x] `index.html`: 화면 폭이 786px 이하로 줄면 본문(`main`)과 사이드바(`aside`)를 가로 배치에서 세로 1열 배치로 전환하고, 바로가기도 세로 1열 배열로 변경
    *   [x] `myTrip.html`: 여행지 Grid 레이아웃을 3열에서 1열(`1fr`)로 유연하게 축소
*   **추가 실습사항**
    *   [x] 모바일 해상도에서 사이드바 내부 실습 버튼들이 세로로 길어지는 왜곡을 방지하기 위해 가로 행 칩 배열(`flex-direction: row`, `flex-wrap: wrap`) 반응형 스타일 추가 튜닝

---

#### CSS 과제 13: 미션6 - 생동감을 불어넣는 애니메이션
*   **기본 요구사항**
    *   [x] 네비게이션 메뉴 및 회원가입 버튼에 `transition` 속성을 적용해 마우스 호버 시 배경색/글자색이 부드럽게 변경되도록 제어
    *   [x] 여행지 앨범 카드 호버 시 위로 떠 오르는 물리 효과(`transform: translateY`) 및 그림자 드리프트(`box-shadow`) 적용
    *   [x] `index.html` 최초 로딩 시 헤더 타이틀이 부드럽게 페이드인되면서 내려오는 등장 애니메이션(`@keyframes`) 구현
*   **추가 실습사항**
    *   [x] 호버 인터랙션 시 화면 떨림을 방지하기 위해 `will-change` 속성을 선제 활용하고 `cubic-bezier` 타이밍 함수 튜닝

---

### 3부. JavaScript 파트 (과제 14 ~ 과제 19)

#### JS 과제 14: Up-Down 숫자 맞추기 게임
*   **기본 요구사항**
    *   [x] `/script/unDown.js` 생성 및 `index.html` 연동
    *   [x] `Math.random()`을 사용하여 1부터 50 사이의 임의 난수를 비밀 숫자로 생성
    *   [x] `prompt()` 창을 띄워 사용자 입력을 루프 돌며 입력받고, 입력값에 따라 "Up!" / "Down!" 알림창(`alert`) 안내
    *   [x] 정답 시도 횟수를 누적하여 "X번 만에 맞추셨습니다." 알림 및 반복문 탈출 (`break`)
    *   [x] `index.html` 내에 게임 시작을 트리거하는 버튼 연동
*   **추가 실습사항**
    *   [x] 사용자 입력 단계에서 문자가 입력되거나(`isNaN`), 취소 버튼 클릭 시(`null`) 루프가 터지지 않도록 예외 수용 및 재입력 유도 방어 코딩 구현

---

#### JS 과제 15: 성적 계산기
*   **기본 요구사항**
    *   [x] `/script/grade.js` 생성 및 `index.html` 연동
    *   [x] `["HTML", "CSS", "JavaScript"]` 과목 배열 순회를 위한 `for` 루프 설계
    *   [x] 각 과목 점수를 `prompt()`로 연속 입력받아 총점(`total`) 누적
    *   [x] 반복 완료 후 평균을 산출하고 60점 이상 합격, 미만 불합격 판정 결과를 브라우저 알림창(`alert`)으로 제공
*   **추가 실습사항**
    *   [x] 100점을 초과하거나 음수 값을 입력하는 오입력 예외 발생 시, 해당 루프 인덱스를 복구하여 다시 입력받도록 재시도 알고리즘 구현

---

#### JS 과제 16: 내 가방 보기
*   **기본 요구사항**
    *   [x] `/script/bag.js` 생성 및 `index.html` 연동
    *   [x] 소지품 데이터를 보관하는 JavaScript 객체(Object) 배열 `myBag` 선언
    *   [x] `showMyBag()` 함수 내부에서 반복문을 통해 가방 속 소지품 목록을 순회 출력
*   **추가 실습사항**
    *   [x] 객체 순회 가독성을 높이기 위해 템플릿 리터럴 문법을 사용해 가공된 문자열 구조를 콘솔(`console.log`) 및 알림창(`alert`)에 다중 포매팅

---

#### JS 과제 17: 실시간 날씨 - DOM/이벤트
*   **기본 요구사항**
    *   [x] `/script/realtimeInfo.js` 연동
    *   [x] 사이드바 영역에 도시 선택 `<select id="city-select">` 및 결과를 담을 `<div id="weather-box">` 생성
    *   [x] 사용자가 도시 선택을 바꿀 때마다 `change` 이벤트를 감지하여 선택한 도시 이름과 위도/경도 좌표를 DOM 조작을 통해 화면에 동적 노출
*   **추가 실습사항**
    *   [x] **HTML5 Geolocation API 연동**: `📍 내 현재 위치 (GPS)` 옵션을 제공해 사용자의 실제 기기 센서 정보를 수집하고 실시간 좌표를 출력하도록 기능 고도화
    *   [x] **State Persistence (상태 지속성)**: `localStorage` API를 활용하여 사용자가 마지막으로 선택한 도시 정보를 브라우저에 저장하고, 브라우저 재실행 시 해당 도시로 자동 복구 실행
    *   [x] **Layout Shift 예방**: 정보 수신 전후로 카드의 상하 높이가 팽창하는 흔들림을 막기 위해 뼈대 레이아웃(Skeleton)을 사전 구축하고 글자 영역만 갱신하도록 설계

---

#### JS 과제 18: 실시간 날씨 - 비동기 호출
*   **기본 요구사항**
    *   [x] `fetch()` 및 `async/await` 문법을 사용해 Open-Meteo 무료 기상 API 호출 구조 작성
    *   [x] 데이터를 수신하는 동안 화면에 `로딩 중... ⏳` 상태를 노출하고 완료 시 실시간 온도와 상대 습도를 화면에 렌더링
*   **추가 실습사항**
    *   [x] **Weather Code 디코딩**: API 응답 내의 `weather_code` 정수 데이터를 분석하는 해독기(`getWeatherState`)를 내장하여 `☀️ 맑음`, `⛅ 구름 조금`, `🌧️ 비`, `❄️ 눈` 등의 구체적인 한국어 상태 텍스트와 전용 이모지 자동 매핑 처리

---

#### JS 과제 19: 실시간 날씨 - 모듈분리
*   **기본 요구사항**
    *   [x] `index.html`에 `<script type="module">` 방식으로 모듈 연결 관계 재설계
    *   [x] API 통신 및 데이터 조회를 전담하는 `weatherAPI.js` 모듈 분리 및 함수 `export` 처리
    *   [x] DOM 제어 및 이벤트를 책임지는 `realtimeInfo.js`에서 API 함수를 `import`하여 관심사 분리(SoC) 완결
*   **추가 실습사항**
    *   [x] 모듈 간 의존성 최소화를 통해 유지보수 가능한 컴포넌트형 구조 수립 및 모듈 격리 스코프 보장
